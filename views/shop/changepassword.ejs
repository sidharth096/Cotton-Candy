<head>
    <style>
        	body {
         background-image: url('/img/login-page-background.jpg');
        background-position:top center; /* Adjust the horizontal and vertical position of the image */
         background-size: cover; /* Scale the image to cover the entire background */
        color: #fcfdff;
    
       }
    </style>
</head>
<%-include('../partials/userheader')-%>
<section class="breadcrumb-option-extra p-0">
    <div class="container ">
      <div class="row">
        <div class="col-lg-12">
          <div class="breadcrumb__text text-light">
            <h4 class=" text-light">Login</h4>
            <div class="breadcrumb__links">
              <a href="/home" class=" text-light">Home</a>
              <span>Login</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

<body>
    <div role="main" class="main shop py-4">
        <div class="container py-4">
            <div class="row justify-content-center mt-5">
                <div class="col-md-6 col-lg-5 mb-5 mb-lg-0 mt-5 bg-white rounded-lg shadow-lg p-3">
                    <h2 class="font-weight-bold text-5 mb-0 text-center">Reset Password</h2>
                    <form action="/resetpasspost/<%=user1._id %>" id="reset-form" method="post" class="needs-validation">
                        <div class="row">
                            <div class="form-group col">
                                <label class="form-label text-color-dark text-3">Password <span
                                        class="text-color-danger">*</span></label>
                                <input type="password" value="" class="form-control form-control-lg text-4"name="password"
                                    id="password1" />
                                <span class="text-danger d-none" style="font-size: 12px; margin-left: 10px"
                                    id="password1Error"></span>
                            </div>
                        </div>
                        <div class="row">
                            <div class="form-group col">
                                <label class="form-label text-color-dark text-3">Confirm Password <span
                                        class="text-color-danger">*</span></label>
                                <input type="password" value="" class="form-control form-control-lg text-4"
                                     id="password2" />
                                <span class="text-danger d-none" style="font-size: 12px; margin-left: 10px"
                                    id="password2Error"></span>
                            </div>
                        </div>

                        <div class="row">
                            <div class="form-group col">
                                <button type="submit"
                                    class="btn btn-dark btn-modern w-100 text-uppercase rounded-0 font-weight-bold text-3 py-3"
                                    data-loading-text="Loading...">
                                    Reset Password
                                </button>

                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Optional JavaScript; choose one of the two! -->

    <!-- Option 1: Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
        crossorigin="anonymous"></script>

    <!-- Option 2: Separate Popper and Bootstrap JS -->
    <!--
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js" integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF" crossorigin="anonymous"></script>
    -->
     
</body>

</html>
<script>
    //regiser validation

    const resetForm = document.getElementById("reset-form");
    //   const email = document.getElementById("email");
    const password1 = document.getElementById("password1");
    const password2 = document.getElementById("password2");

    const passRegX = /^[a-zA-Z0-9!@#$%^&*()_+\-=[\]{};':"\\|,.<>/?]{6,}$/;

    resetForm.addEventListener("submit", (e) => {
        //     if (email.value === "" || email.value === null) {
        //       e.preventDefault();
        //       document.getElementById("emailError").classList.remove("d-none");
        //       document.getElementById("emailError").innerHTML = "Email must be filled!";
        //     } else if (!email.value.match(emailRegX)) {
        //       e.preventDefault();
        //       document.getElementById("emailError").classList.remove("d-none");
        //       document.getElementById("emailError").innerHTML = "Invalid Email Address";
        //     } else if (email.value.match(emailRegX)) {
        //       document.getElementById("emailError").classList.remove("d-none");
        //       document.getElementById("emailError").innerHTML = "";
        //     }

        if (password1.value === "" || password1.value === null) {
            e.preventDefault();
            document.getElementById("password1Error").classList.remove("d-none");
            document.getElementById("password1Error").innerHTML =
                "password number must be filled!"; r
        } else if (!password1.value.match(passRegX)) {
            e.preventDefault();
            document.getElementById("password1Error").classList.remove("d-none");
            document.getElementById("password1Error").innerHTML =
                "Password must contain at least 6 characters ";
        } else if (password1.value.match(passRegX)) {
            document.getElementById("password1Error").classList.remove("d-none");
            document.getElementById("password1Error").innerHTML = "";
        }

        if (password2.value === "" || password2.value === null) {
            e.preventDefault();
            document.getElementById("password2Error").classList.remove("d-none");
            document.getElementById("password2Error").innerHTML =
                "password number must be filled!"; r
        } else if (!password2.value.match(passRegX)) {
            e.preventDefault();
            document.getElementById("password2Error").classList.remove("d-none");
            document.getElementById("password2Error").innerHTML =
                "Password must contain at least 6 characters ";
        } else if (password2.value.match(passRegX)) {
            document.getElementById("password2Error").classList.remove("d-none");
            document.getElementById("password2Error").innerHTML = "";
        }

          if (password.value !== password2.value) {
       e.preventDefault();
       document.getElementById("password2Error").classList.remove("d-none");
       document.getElementById("password2Error").innerHTML =
         "Passwords do not match!";
     } else {
       document.getElementById("password2Error").classList.remove("d-none");
       document.getElementById("password2Error").innerHTML = "";
     }
    });
</script> 